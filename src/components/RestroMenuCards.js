import React from "react";
const RestroMenuCards = (props) => {
  console.log(
    restaurantinfo?.cards[2]?.groupedCard?.cardGroupMap?.REGULAR?.cards
  );
  return <h1>{props.data}</h1>;
};
export default RestroMenuCards;
